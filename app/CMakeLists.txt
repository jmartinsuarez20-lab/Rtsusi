cmake_minimum_required(VERSION 3.22.1)

project(AppCMakeStub LANGUAGES CXX)

# Este archivo es un stub para satisfacer la búsqueda de CMake en la raíz del módulo.
# La compilación C/C++ real para java-llama.cpp se maneja por su propio CMakeLists.txt
# y la configuración en build.gradle.kts que apunta a él.
